import { GenreValidatorDirective } from './genre-validator.directive';

describe('GenreValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new GenreValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
